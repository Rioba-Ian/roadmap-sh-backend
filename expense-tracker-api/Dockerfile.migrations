FROM migrate/migrate:v4.19.1

WORKDIR /app

# Copy migration files
COPY db/migrations ./migrations

# Create a script to convert goose format to migrate format and run migrations
CMD ["sh", "-c", "migrate -source ./migrations -database \"$GOOSE_DBSTRING\" up"]
